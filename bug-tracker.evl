context Product {
	constraint C1 {
		check: self.possibleVersion.includes(self.currentVersion)
		message: self.currentVersion + " is not in the specified versions: " + self.possibleVersion
	}

/*	constraint C1 {
		check: self.anton <> "hi" 
		message: "oi oi"
	}
*/
}

